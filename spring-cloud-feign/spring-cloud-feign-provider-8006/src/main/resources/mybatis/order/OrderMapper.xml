<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.spring.family.springcloudfeignprovider8006.dao.OrderMapper">

    <select id="getResourceInfo" parameterType="String" resultType="map">
          select * from m_resources where res_code=#{resCode}
    </select>
</mapper>